name: FRAS CI/CD Pipeline

on:
  push:
    branches:
      - main
      - "FRAS-*"
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Run tests
        run: echo "✅ Running tests for FRAS project"

      - name: Deploy
        run: echo "🚀 Simulated deployment for Jira integration"
